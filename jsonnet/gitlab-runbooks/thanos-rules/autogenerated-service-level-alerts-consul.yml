# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./thanos-rules-jsonnet/service-component-alerts.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: 'Service Component Alerts: consul'
  interval: 1m
  partial_response_strategy: warn
  rules:
  - alert: ConsulServiceConsulTrafficCessation
    for: 5m
    annotations:
      title: The consul SLI of the consul service (`{{ $labels.stage }}` stage) has
        not received any traffic in the past 30m
      description: |
        HTTP GET requests handled by the Consul agent.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4242308695"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          clamp_min(deriv(consul_http_GET_v1_agent_metrics_count{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m]), 0)
        )
      runbook: docs/consul/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_component_ops:rate_30m{component="consul",monitor="global",stage="main",type="consul"} == 0
      and
      gitlab_component_ops:rate_30m{component="consul",monitor="global",stage="main",type="consul"} offset 1h >= 0.16666666666666666
  - alert: ConsulServiceConsulTrafficAbsent
    for: 30m
    annotations:
      title: The consul SLI of the consul service (`{{ $labels.stage }}` stage) has
        not reported any traffic in the past 30m
      description: |
        HTTP GET requests handled by the Consul agent.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4242308695"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          clamp_min(deriv(consul_http_GET_v1_agent_metrics_count{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}"}[5m]), 0)
        )
      runbook: docs/consul/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_component_ops:rate_5m{component="consul",monitor="global",stage="main",type="consul"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="consul",monitor="global",stage="main",type="consul"}
  - alert: ConsulServiceConsulTrafficCessationRegional
    for: 5m
    annotations:
      title: The consul SLI of the consul service in region `{{ $labels.region }}`
        has not received any traffic in the past 30m
      description: |
        HTTP GET requests handled by the Consul agent.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4242308695"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          clamp_min(deriv(consul_http_GET_v1_agent_metrics_count{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m]), 0)
        )
      runbook: docs/consul/README.md
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_regional_sli_ops:rate_30m{component="consul",monitor="global",stage="main",type="consul"} == 0
      and
      gitlab_regional_sli_ops:rate_30m{component="consul",monitor="global",stage="main",type="consul"} offset 1h >= 0.16666666666666666
  - alert: ConsulServiceConsulTrafficAbsentRegional
    for: 30m
    annotations:
      title: The consul SLI of the consul service in region `{{ $labels.region }}`
        has not reported any traffic in the past 30m
      description: |
        HTTP GET requests handled by the Consul agent.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.

        Note that this alert is specific to the `{{ $labels.region }}` region.
      grafana_dashboard_id: consul-main/consul-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/consul-main/consul-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4242308695"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage,region) (
          clamp_min(deriv(consul_http_GET_v1_agent_metrics_count{environment="{{ $labels.environment }}",region="{{ $labels.region }}",stage="{{ $labels.stage }}"}[5m]), 0)
        )
      runbook: docs/consul/README.md
    labels:
      aggregation: regional_component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "no"
    expr: |
      gitlab_regional_sli_ops:rate_5m{component="consul",monitor="global",stage="main",type="consul"} offset 1h
      unless
      gitlab_regional_sli_ops:rate_5m{component="consul",monitor="global",stage="main",type="consul"}
