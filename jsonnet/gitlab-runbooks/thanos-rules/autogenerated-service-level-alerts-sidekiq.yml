# WARNING. DO NOT EDIT THIS FILE BY HAND. USE ./thanos-rules-jsonnet/service-component-alerts.jsonnet TO GENERATE IT
# YOUR CHANGES WILL BE OVERRIDDEN
groups:
- name: 'Service Component Alerts: sidekiq'
  interval: 1m
  partial_response_strategy: warn
  rules:
  - alert: SidekiqServiceEmailReceiverErrorSLOViolation
    for: 2m
    annotations:
      title: The email_receiver SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        Monitors ratio between all received emails and received emails which could not be processed for some reason.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "212053046"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_transaction_event_email_receiver_error_total{environment="{{ $labels.environment }}",error!="Gitlab::Email::AutoGeneratedEmailError",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: service_desk
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="email_receiver",monitor="global",type="sidekiq"}
          > (14.4 * 0.300000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="email_receiver",monitor="global",type="sidekiq"}
          > (14.4 * 0.300000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="email_receiver",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceEmailReceiverErrorSLOViolation
    for: 2m
    annotations:
      title: The email_receiver SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        Monitors ratio between all received emails and received emails which could not be processed for some reason.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "212053046"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_transaction_event_email_receiver_error_total{environment="{{ $labels.environment }}",error!="Gitlab::Email::AutoGeneratedEmailError",stage="{{ $labels.stage }}"}[5m])
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: service_desk
      rules_domain: general
      severity: s3
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="email_receiver",monitor="global",type="sidekiq"}
          > (6 * 0.300000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="email_receiver",monitor="global",type="sidekiq"}
          > (6 * 0.300000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="email_receiver",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceEmailReceiverTrafficCessation
    for: 5m
    annotations:
      title: The email_receiver SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has not received any traffic in the past 30m
      description: |
        Monitors ratio between all received emails and received emails which could not be processed for some reason.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1545803555"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(sidekiq_jobs_completion_seconds_count{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",worker=~"EmailReceiverWorker|ServiceDeskEmailReceiverWorker"}[5m])
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: service_desk
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="email_receiver",monitor="global",stage="main",type="sidekiq"} == 0
      and
      gitlab_component_ops:rate_30m{component="email_receiver",monitor="global",stage="main",type="sidekiq"} offset 1h >= 0.16666666666666666
  - alert: SidekiqServiceEmailReceiverTrafficAbsent
    for: 30m
    annotations:
      title: The email_receiver SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has not reported any traffic in the past 30m
      description: |
        Monitors ratio between all received emails and received emails which could not be processed for some reason.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1545803555"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(sidekiq_jobs_completion_seconds_count{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",worker=~"EmailReceiverWorker|ServiceDeskEmailReceiverWorker"}[5m])
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: service_desk
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="email_receiver",monitor="global",stage="main",type="sidekiq"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="email_receiver",monitor="global",stage="main",type="sidekiq"}
  - alert: SidekiqServiceGlobalSearchIndexingApdexSLOViolation
    for: 2m
    annotations:
      title: The global_search_indexing SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        The number of Global Search indexing calls meeting their duration target based on the 99.95th percentile of indexing. This indicates the duration between when an item was changed and when it became available in Elasticsearch.

        The target duration can be found here: https://gitlab.com/gitlab-org/gitlab/-/blob/master/ee/lib/gitlab/metrics/global_search_indexing_slis.rb#L14-L15

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4092777115"
      grafana_variables: environment,stage
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="global_search_indexing",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="global_search_indexing",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="global_search_indexing",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceGlobalSearchIndexingApdexSLOViolation
    for: 2m
    annotations:
      title: The global_search_indexing SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        The number of Global Search indexing calls meeting their duration target based on the 99.95th percentile of indexing. This indicates the duration between when an item was changed and when it became available in Elasticsearch.

        The target duration can be found here: https://gitlab.com/gitlab-org/gitlab/-/blob/master/ee/lib/gitlab/metrics/global_search_indexing_slis.rb#L14-L15

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4092777115"
      grafana_variables: environment,stage
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="global_search_indexing",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="global_search_indexing",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="global_search_indexing",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceGlobalSearchIndexingTrafficCessation
    for: 5m
    annotations:
      title: The global_search_indexing SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has not received any traffic in the past 30m
      description: |
        The number of Global Search indexing calls meeting their duration target based on the 99.95th percentile of indexing. This indicates the duration between when an item was changed and when it became available in Elasticsearch.

        The target duration can be found here: https://gitlab.com/gitlab-org/gitlab/-/blob/master/ee/lib/gitlab/metrics/global_search_indexing_slis.rb#L14-L15

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2184528438"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_global_search_indexing_apdex_total{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="sidekiq"}[5m])
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="global_search_indexing",monitor="global",stage="main",type="sidekiq"} == 0
      and
      gitlab_component_ops:rate_30m{component="global_search_indexing",monitor="global",stage="main",type="sidekiq"} offset 1h >= 0.16666666666666666
  - alert: SidekiqServiceGlobalSearchIndexingTrafficAbsent
    for: 30m
    annotations:
      title: The global_search_indexing SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has not reported any traffic in the past 30m
      description: |
        The number of Global Search indexing calls meeting their duration target based on the 99.95th percentile of indexing. This indicates the duration between when an item was changed and when it became available in Elasticsearch.

        The target duration can be found here: https://gitlab.com/gitlab-org/gitlab/-/blob/master/ee/lib/gitlab/metrics/global_search_indexing_slis.rb#L14-L15

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2184528438"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          rate(gitlab_sli_global_search_indexing_apdex_total{environment="{{ $labels.environment }}",stage="{{ $labels.stage }}",type="sidekiq"}[5m])
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: global_search
      rules_domain: general
      severity: s3
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="global_search_indexing",monitor="global",stage="main",type="sidekiq"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="global_search_indexing",monitor="global",stage="main",type="sidekiq"}
  - alert: SidekiqServiceShardCatchallApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_catchall SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the catchall Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4125397901"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_catchall",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_catchall",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_catchall",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardCatchallApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_catchall SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the catchall Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4125397901"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_catchall",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_catchall",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_catchall",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardCatchallErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_catchall SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the catchall Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "532783926"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_catchall",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_catchall",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_catchall",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardCatchallErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_catchall SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the catchall Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "532783926"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="catchall",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_catchall",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_catchall",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_catchall",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardCatchallTrafficCessation
    for: 5m
    annotations:
      title: The shard_catchall SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has not received any traffic in the past 30m
      description: |
        Aggregation of all jobs for the catchall Sidekiq shard.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2518816313"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",le="+Inf",shard="catchall",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="shard_catchall",monitor="global",stage="main",type="sidekiq"} == 0
      and
      gitlab_component_ops:rate_30m{component="shard_catchall",monitor="global",stage="main",type="sidekiq"} offset 1h >= 0.16666666666666666
  - alert: SidekiqServiceShardCatchallTrafficAbsent
    for: 30m
    annotations:
      title: The shard_catchall SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has not reported any traffic in the past 30m
      description: |
        Aggregation of all jobs for the catchall Sidekiq shard.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2518816313"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",le="+Inf",shard="catchall",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="shard_catchall",monitor="global",stage="main",type="sidekiq"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="shard_catchall",monitor="global",stage="main",type="sidekiq"}
  - alert: SidekiqServiceShardDatabaseThrottledApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_database_throttled SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the database-throttled Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "564185305"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_database_throttled",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_database_throttled",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_database_throttled",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardDatabaseThrottledApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_database_throttled SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the database-throttled Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "564185305"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_database_throttled",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_database_throttled",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_database_throttled",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardDatabaseThrottledErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_database_throttled SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the database-throttled Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1659464052"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_database_throttled",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_database_throttled",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_database_throttled",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardDatabaseThrottledErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_database_throttled SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the database-throttled Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1659464052"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="database-throttled",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_database_throttled",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_database_throttled",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_database_throttled",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardElasticsearchApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_elasticsearch SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the elasticsearch Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2299339214"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_elasticsearch",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_elasticsearch",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_elasticsearch",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardElasticsearchApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_elasticsearch SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the elasticsearch Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2299339214"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_elasticsearch",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_elasticsearch",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_elasticsearch",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardElasticsearchErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_elasticsearch SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the elasticsearch Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "389908901"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_elasticsearch",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_elasticsearch",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_elasticsearch",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardElasticsearchErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_elasticsearch SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the elasticsearch Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "389908901"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="elasticsearch",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_elasticsearch",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_elasticsearch",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_elasticsearch",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardGitalyThrottledApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_gitaly_throttled SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the gitaly-throttled Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1517683275"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardGitalyThrottledApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_gitaly_throttled SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the gitaly-throttled Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1517683275"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardGitalyThrottledErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_gitaly_throttled SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the gitaly-throttled Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4031214920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardGitalyThrottledErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_gitaly_throttled SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the gitaly-throttled Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "4031214920"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="gitaly-throttled",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "no"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_gitaly_throttled",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardImportsApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_imports SLI of the sidekiq service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        Aggregation of all jobs for the imports Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3422932483"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_imports",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_imports",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_imports",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardImportsApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_imports SLI of the sidekiq service (`{{ $labels.stage }}` stage)
        has an apdex violating SLO
      description: |
        Aggregation of all jobs for the imports Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3422932483"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_imports",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_imports",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_imports",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardImportsErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_imports SLI of the sidekiq service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Aggregation of all jobs for the imports Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1270469885"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_imports",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_imports",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_imports",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardImportsErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_imports SLI of the sidekiq service (`{{ $labels.stage }}` stage)
        has an error rate violating SLO
      description: |
        Aggregation of all jobs for the imports Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1270469885"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="imports",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_imports",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_imports",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_imports",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardLowUrgencyCpuBoundApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_low_urgency_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the low-urgency-cpu-bound Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1949742512"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardLowUrgencyCpuBoundApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_low_urgency_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the low-urgency-cpu-bound Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "1949742512"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardLowUrgencyCpuBoundErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_low_urgency_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the low-urgency-cpu-bound Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "230682702"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardLowUrgencyCpuBoundErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_low_urgency_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the low-urgency-cpu-bound Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "230682702"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_low_urgency_cpu_bound",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardLowUrgencyCpuBoundTrafficCessation
    for: 5m
    annotations:
      title: The shard_low_urgency_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has not received any traffic in the past 30m
      description: |
        Aggregation of all jobs for the low-urgency-cpu-bound Sidekiq shard.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3240333014"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",le="+Inf",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="shard_low_urgency_cpu_bound",monitor="global",stage="main",type="sidekiq"} == 0
      and
      gitlab_component_ops:rate_30m{component="shard_low_urgency_cpu_bound",monitor="global",stage="main",type="sidekiq"} offset 1h >= 0.16666666666666666
  - alert: SidekiqServiceShardLowUrgencyCpuBoundTrafficAbsent
    for: 30m
    annotations:
      title: The shard_low_urgency_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has not reported any traffic in the past 30m
      description: |
        Aggregation of all jobs for the low-urgency-cpu-bound Sidekiq shard.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3240333014"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",le="+Inf",shard="low-urgency-cpu-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="shard_low_urgency_cpu_bound",monitor="global",stage="main",type="sidekiq"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="shard_low_urgency_cpu_bound",monitor="global",stage="main",type="sidekiq"}
  - alert: SidekiqServiceShardMemoryBoundApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_memory_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the memory-bound Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "546338609"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_memory_bound",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.080000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_memory_bound",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.080000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_memory_bound",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardMemoryBoundApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_memory_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the memory-bound Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "546338609"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_memory_bound",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.080000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_memory_bound",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.080000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_memory_bound",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardMemoryBoundErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_memory_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the memory-bound Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2190820353"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_memory_bound",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_memory_bound",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_memory_bound",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardMemoryBoundErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_memory_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the memory-bound Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2190820353"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="memory-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_memory_bound",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_memory_bound",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_memory_bound",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardQuarantineApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_quarantine SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the quarantine Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "838516108"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_quarantine",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_quarantine",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_quarantine",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardQuarantineApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_quarantine SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the quarantine Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "838516108"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_quarantine",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_quarantine",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_quarantine",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardQuarantineErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_quarantine SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the quarantine Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2307979617"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_quarantine",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_quarantine",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_quarantine",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardQuarantineErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_quarantine SLI of the sidekiq service (`{{ $labels.stage }}`
        stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the quarantine Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2307979617"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="quarantine",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_quarantine",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_quarantine",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_quarantine",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardUrgentAuthorizedProjectsApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_authorized_projects SLI of the sidekiq service (`{{
        $labels.stage }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the urgent-authorized-projects Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3650911195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardUrgentAuthorizedProjectsApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_authorized_projects SLI of the sidekiq service (`{{
        $labels.stage }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the urgent-authorized-projects Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3650911195"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardUrgentAuthorizedProjectsErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_authorized_projects SLI of the sidekiq service (`{{
        $labels.stage }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the urgent-authorized-projects Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3367299711"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardUrgentAuthorizedProjectsErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_authorized_projects SLI of the sidekiq service (`{{
        $labels.stage }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the urgent-authorized-projects Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "3367299711"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="urgent-authorized-projects",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_urgent_authorized_projects",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardUrgentCpuBoundApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the urgent-cpu-bound Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "627077500"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardUrgentCpuBoundApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the urgent-cpu-bound Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "627077500"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardUrgentCpuBoundErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the urgent-cpu-bound Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "953665318"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardUrgentCpuBoundErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the urgent-cpu-bound Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "953665318"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="urgent-cpu-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_urgent_cpu_bound",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardUrgentCpuBoundTrafficCessation
    for: 5m
    annotations:
      title: The shard_urgent_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has not received any traffic in the past 30m
      description: |
        Aggregation of all jobs for the urgent-cpu-bound Sidekiq shard.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "659062113"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",le="+Inf",shard="urgent-cpu-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="shard_urgent_cpu_bound",monitor="global",stage="main",type="sidekiq"} == 0
      and
      gitlab_component_ops:rate_30m{component="shard_urgent_cpu_bound",monitor="global",stage="main",type="sidekiq"} offset 1h >= 0.16666666666666666
  - alert: SidekiqServiceShardUrgentCpuBoundTrafficAbsent
    for: 30m
    annotations:
      title: The shard_urgent_cpu_bound SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has not reported any traffic in the past 30m
      description: |
        Aggregation of all jobs for the urgent-cpu-bound Sidekiq shard.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "659062113"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",le="+Inf",shard="urgent-cpu-bound",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="shard_urgent_cpu_bound",monitor="global",stage="main",type="sidekiq"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="shard_urgent_cpu_bound",monitor="global",stage="main",type="sidekiq"}
  - alert: SidekiqServiceShardUrgentOtherApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_other SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the urgent-other Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "203761782"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_1h{component="shard_urgent_other",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_5m{component="shard_urgent_other",monitor="global",type="sidekiq"}
          < (1 - 14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_urgent_other",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardUrgentOtherApdexSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_other SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an apdex violating SLO
      description: |
        Aggregation of all jobs for the urgent-other Sidekiq shard.

        Currently the apdex value is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "203761782"
      grafana_variables: environment,stage
      promql_template_1: |
        histogram_quantile(
          0.950000,
          sum by (env,environment,tier,stage,le) (
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="high"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_queue_duration_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="low"}
            or
            sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}",urgency="throttled"}
          )
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: apdex
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_apdex:ratio_6h{component="shard_urgent_other",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
        and
        (
          gitlab_component_apdex:ratio_30m{component="shard_urgent_other",monitor="global",type="sidekiq"}
          < (1 - 6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_urgent_other",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardUrgentOtherErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_other SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the urgent-other Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2399846707"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 1h
    expr: |
      (
        (
          gitlab_component_errors:ratio_1h{component="shard_urgent_other",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_5m{component="shard_urgent_other",monitor="global",type="sidekiq"}
          > (14.4 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_1h{component="shard_urgent_other",monitor="global",type="sidekiq"}) >= 1
      )
  - alert: SidekiqServiceShardUrgentOtherErrorSLOViolation
    for: 2m
    annotations:
      title: The shard_urgent_other SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has an error rate violating SLO
      description: |
        Aggregation of all jobs for the urgent-other Sidekiq shard.

        Currently the error-rate is {{ $value | humanizePercentage }}.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "2399846707"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_failed_total_rate5m{environment="{{ $labels.environment }}",shard="urgent-other",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: slo_violation
      alert_type: symptom
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: error
      slo_alert: "yes"
      user_impacting: "yes"
      window: 6h
    expr: |
      (
        (
          gitlab_component_errors:ratio_6h{component="shard_urgent_other",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
        and
        (
          gitlab_component_errors:ratio_30m{component="shard_urgent_other",monitor="global",type="sidekiq"}
          > (6 * 0.005000)
        )
      )
      and on(env,environment,tier,type,stage,component)
      (
        sum by(env,environment,tier,type,stage,component) (gitlab_component_ops:rate_6h{component="shard_urgent_other",monitor="global",type="sidekiq"}) >= 0.16667
      )
  - alert: SidekiqServiceShardUrgentOtherTrafficCessation
    for: 5m
    annotations:
      title: The shard_urgent_other SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has not received any traffic in the past 30m
      description: |
        Aggregation of all jobs for the urgent-other Sidekiq shard.

        This alert signifies that the SLI is reporting a cessation of traffic; the signal is present, but is zero.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "907052725"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",le="+Inf",shard="urgent-other",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_30m{component="shard_urgent_other",monitor="global",stage="main",type="sidekiq"} == 0
      and
      gitlab_component_ops:rate_30m{component="shard_urgent_other",monitor="global",stage="main",type="sidekiq"} offset 1h >= 0.16666666666666666
  - alert: SidekiqServiceShardUrgentOtherTrafficAbsent
    for: 30m
    annotations:
      title: The shard_urgent_other SLI of the sidekiq service (`{{ $labels.stage
        }}` stage) has not reported any traffic in the past 30m
      description: |
        Aggregation of all jobs for the urgent-other Sidekiq shard.

        This alert signifies that the SLI was previously reporting traffic, but is no longer - the signal is absent.

        This could be caused by a change to the metrics used in the SLI, or by the service not receiving traffic.
      grafana_dashboard_id: sidekiq-main/sidekiq-overview
      grafana_dashboard_link: https://dashboards.gitlab.net/d/sidekiq-main/sidekiq-overview?from=now-6h/m&to=now-1m/m&var-environment={{
        $labels.environment }}&var-stage={{ $labels.stage }}
      grafana_min_zoom_hours: "6"
      grafana_panel_id: "907052725"
      grafana_variables: environment,stage
      promql_template_1: |
        sum by (env,environment,tier,stage) (
          sli_aggregations:sidekiq_jobs_completion_seconds_bucket_rate5m{environment="{{ $labels.environment }}",le="+Inf",shard="urgent-other",stage="{{ $labels.stage }}"}
        )
      runbook: docs/sidekiq/README.md
    labels:
      aggregation: component
      alert_class: traffic_cessation
      alert_type: cause
      feature_category: not_owned
      pager: pagerduty
      rules_domain: general
      severity: s2
      sli_type: ops
      slo_alert: "no"
      user_impacting: "yes"
    expr: |
      gitlab_component_ops:rate_5m{component="shard_urgent_other",monitor="global",stage="main",type="sidekiq"} offset 1h
      unless
      gitlab_component_ops:rate_5m{component="shard_urgent_other",monitor="global",stage="main",type="sidekiq"}
