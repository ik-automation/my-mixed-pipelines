# source:
# - https://github.com/search?o=desc&q=hmarr%2Fauto-approve-action+path%3A.github%2Fworkflows+language%3AYAML&s=&type=Code
# - https://github.com/aws-samples/serverless-webapp-mono-repo-ci-cd-java/blob/master/.github/workflows/build-backend-infra.yml
# tags: ['github action', 'auto approve', 'approve']


# ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".

name: AutoApprove
on:
  pull_request_target:
    types:
      - assigned
      - opened
      - synchronize
      - reopened
jobs:
  auto-approve:
    runs-on: ubuntu-latest
    steps:
      - uses: hmarr/auto-approve-action@v2.0.0
        if: github.actor == 'pahud' || contains(
          github.event.pull_request.labels.*.name, 'auto-approve')
        if: >
          github.actor == 'dependabot[bot]' || github.actor == 'dependabot-preview[bot]'
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}